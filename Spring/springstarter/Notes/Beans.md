### Beans

###### Beans just the object which created at the run time of spring code.

###### The Spring IoC container is responsible for creating objects (beans).

###### You define beans in configuration (XML, Java annotations, or Java config).
