### Github branch flow for project development:

```
main <- stage merge with main
stage <- QA merge (shown to client)
QA <- text branch merge
test <- first push <- feature branch(development branch)
```